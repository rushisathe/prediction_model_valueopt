install yfinance , pandas-reader using following commands
!pip install yfinance --upgrade --no-cache-dir
!pip install keras
!pip install tensorflow

For input symbol use .NS extension for NSE stocks.
